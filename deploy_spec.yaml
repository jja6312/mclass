version: 1.0
component: deployment
files:  
  - source: /demo-0.0.1-SNAPSHOT.jar
    destination: /tmp/
steps:
  - stepType: Command
    name: Start Application
    command:  nohup java -jar /tmp/demo-0.0.1-SNAPSHOT.jar > /tmp/app.log 2>&1 &
    timeoutInSeconds: 60